- 1) Dado `xs` devuelve el MCM de todos los elementos de la lista
   ```haskell
   mcmLista :: [Int] -> Int
   ```
- 2) Dados `(c,a,b)` devuelve `(s,t)` tal que `c < as + bt`
  ```haskell
  solEcLineal :: (Int, Int, Int) -> (Int, Int)
  ```
